name: 'client payload name'
description: 'get client payload name'
inputs:
  client-payload:  # id of input
    description: 'name of client payload'
    required: true
runs:
  using: 'node12'
  main: 'index.js'
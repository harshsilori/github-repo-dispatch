name: example-client-payload-action
on: repository_dispatch
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: 'echo "field2: ${{ github.event.client_payload.baz }}"'
      - run: 'echo "payload2: ${{ toJson(github.event.client_payload) }}"'
      - run: echo unsuccessfull
        if: github.event.action == 'baz'
